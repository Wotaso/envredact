{
  "name": "envredact",
  "version": "1.0.0",
  "private": true,
  "description": "Static frontend for redacting sensitive .env variables locally in the browser.",
  "scripts": {
    "start": "http-server . -p ${PORT:-3000} -c-1"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
